---
title: "下载视频（OpenAI 风格）"
description: "使用 GET /v1/videos/{id}/content 下载成片（支持断点重传）。"
openapi: 'GET /v1/videos/{video_id}/content'
---

下载接口会将上游真实地址进行代理，返回标准的视频文件流。请在任务 `completed` 后再调用。

## 认证

- `Authorization: Bearer <Token>`

```bash
export BASE_URL="https://models.kapon.cloud/v1"
export TOKEN="oh-xxxxxxxxxxxxxxxx"
export VIDEO_ID="video_xxx"  # 已完成的任务 id
```

## cURL 示例

```bash
curl -L "$BASE_URL/videos/$VIDEO_ID/content" \
  -H "Authorization: Bearer $TOKEN" \
  --output out.mp4
```

<Callout type="tip">
  如果你在浏览器中请求该地址，将直接触发下载（或在内嵌播放器中播放）。
</Callout>

