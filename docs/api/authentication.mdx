---
title: "API 认证"
description: "如何获取与使用访问凭证"
---

本页说明调用 One Hub API 的认证流程（示例占位）：

1. 在控制台创建 API Key（示例路径：设置 → 开发者 → API Keys）
2. 调用时在请求头添加：

```
Authorization: Bearer <YOUR_API_KEY>
```

3. 建议将密钥存放于安全的服务端环境变量，不要在客户端暴露。

如需 OAuth 或细粒度权限策略，可在此补充具体配置与示例代码。

